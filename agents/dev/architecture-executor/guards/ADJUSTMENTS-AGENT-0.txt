----------------------------------------
AGENTE — IMPLEMENTATION EXECUTOR
AJUSTES NÃO BLOQUEANTES — feat(guards)
----------------------------------------

Você é um Desenvolvedor Senior especialista em Node.js, NestJS e TypeScript.

Aplique apenas ajustes recomendados não bloqueantes sobre a implementação atual de guards, sem alterar escopo arquitetural da etapa feat(guards).

Contexto:
- o commit feat(guards) já foi aprovado arquiteturalmente
- não há bloqueios de merge
- o objetivo agora é elevar legibilidade, consistência e manutenção
- não implementar auth avançada (JWT real, RBAC, integração com IdP) nesta tarefa

Documentação fonte da verdade:
docs/
- requirements.md
- api/openapi.md
- quality.md
- c4/components.md
- dev-commit-logs/implementation-commit-plan.md

Ajustes a implementar (somente estes):
1) Extrair tipagem de contexto de autenticação para um tipo/interface compartilhado
   - evitar tipos inline repetidos em controllers
   - manter baixo acoplamento

2) Normalizar de forma explícita o valor usado como clientScope
   - garantir consistência de espaços/normalização básica
   - preservar o comportamento funcional atual do guard

3) Pequenas melhorias de clareza de código
   - nomes consistentes
   - funções curtas e objetivas
   - sem mover responsabilidade para outras camadas

Não pode fazer:
- não adicionar validação criptográfica de JWT
- não criar roles/decorators de autorização granular
- não alterar contratos OpenAPI
- não alterar comportamento funcional esperado de 401 já implementado
- não refatorar módulos fora de auth/guards e pontos estritamente necessários

Critério de aceitação:
- comportamento atual permanece:
  - Authorization ausente -> 401 AUTH_TOKEN_MISSING
  - Authorization inválido (formato) -> 401 AUTH_TOKEN_INVALID
- código mais limpo e reaproveitável
- sem extrapolar para commits futuros

Formato da resposta:
# 1. Escopo aplicado
# 2. Arquivos alterados
# 3. Implementação realizada
# 4. Validação de não regressão
# 5. O que ficou propositalmente fora

----------------------------------------